<% const user = currentUser; %>
<% const isAdmin = user && (user.role === 'ADMIN' || user.role === 'SUPER_ADMIN'); %>
<% const isSuper = user && user.role === 'SUPER_ADMIN'; %>
<% const roleLabel = user ? (user.role === 'SUPER_ADMIN' ? t('roleSuper') : user.role === 'ADMIN' ? t('roleAdmin') : t('workersTitle')) : ''; %>
<% const canAccessModule = (moduleKey, action = 'view') => (typeof canModuleAccess === 'function' ? canModuleAccess(moduleKey, action) : true); %>
<header class="app-header sticky top-0 z-50 border-b border-slate-200/80 bg-white/95 backdrop-blur shadow-sm">
  <div class="mx-auto flex w-full max-w-6xl flex-wrap items-center justify-between gap-4 px-4 py-3">
    <div class="flex items-center gap-3">
      <a class="flex items-center gap-3" href="/">
        <div class="flex h-10 w-10 items-center justify-center overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">
          <% if (logoPath) { %>
            <img src="<%= logoPath %>" alt="logo" class="h-10 w-10 object-contain" />
          <% } else { %>
            <span class="text-sm font-bold text-aqua-700">A</span>
          <% } %>
        </div>
        <div>
          <p class="text-lg font-semibold">AQUA MSK</p>
          <p class="text-xs text-slate-500"><%= t('brandTagline') %></p>
        </div>
      </a>
      <% if (isSuper) { %>
        <details class="relative">
          <summary class="btn-outline text-xs"><%= t('editBranding') %></summary>
          <div class="absolute left-0 mt-2 w-72 card p-4 shadow-lg">
            <p class="text-xs font-semibold text-slate-600"><%= t('logoImageTitle') %></p>
            <form method="post" action="/admin/logo" enctype="multipart/form-data" class="mt-2 space-y-2">
              <input class="input h-9" type="file" name="logo_file" accept="image/*" required />
              <button class="btn-outline w-full" type="submit"><%= t('uploadLogo') %></button>
            </form>
            <% if (logoPath) { %>
              <form method="post" action="/admin/logo/delete" class="mt-2">
                <button class="btn-danger w-full" type="submit"><%= t('deleteLogo') %></button>
              </form>
            <% } %>
          </div>
        </details>
      <% } %>
    </div>

    <div class="flex items-center gap-2">
      <% if (user) { %>
        <button id="menuToggle" class="btn-outline" type="button" aria-haspopup="true" aria-expanded="false">
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
          <span class="ml-2 text-xs font-semibold"><%= t('menu') %></span>
        </button>
      <% } %>
      <div class="hidden text-right sm:block">
        <p class="text-sm font-semibold"><%= user ? user.full_name : '' %></p>
        <p class="text-xs text-slate-500"><%= roleLabel %></p>
      </div>
      <form method="post" action="/logout" onsubmit="return confirm('<%= t('logoutConfirm') %>');">
        <button class="btn-outline" type="submit">Logout</button>
      </form>
    </div>
  </div>
</header>

<% if (user) { %>
  <div id="menuPanel" class="fixed inset-0 z-50 hidden" hidden>
    <div class="absolute inset-0 bg-slate-900/30" data-close></div>
    <div class="absolute right-4 top-4 w-full max-w-sm drawer p-4">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-sm font-semibold"><%= t('menu') %></p>
          <p class="text-xs text-slate-500">AQUA MSK</p>
        </div>
        <button class="btn-outline" type="button" data-close><%= t('close') %></button>
      </div>
      <div class="mt-4 text-sm menu-accordion">
        <% if (isAdmin) { %>
          <details class="menu-section" open>
            <summary class="menu-section-summary">
              <span><%= t('dashboard') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <a class="btn-outline" href="/admin"><%= t('dashboard') %></a>
                <a class="btn-outline" href="/search"><%= t('search') %></a>
                <a class="btn-outline" href="/admin/alerts"><%= t('alertCenterTitle') %></a>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuWorkforce') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <a class="btn-outline" href="/admin/workers"><%= t('workers') %></a>
                <% if (canAccessModule('staffs')) { %><a class="btn-outline" href="/admin/staffs"><%= t('staffsTitle') %></a><% } %>
                <% if (canAccessModule('staffs')) { %><a class="btn-outline" href="/admin/staff-roles"><%= t('manageStaffRoles') %></a><% } %>
                <% if (canAccessModule('attendance')) { %><a class="btn-outline" href="/admin/worker-attendance"><%= t('workerAttendanceTitle') %></a><% } %>
                <a class="btn-outline" href="/admin/profile"><%= t('profileTitle') %></a>
                <% if (isSuper) { %><a class="btn-outline" href="/admin/admins"><%= t('admins') %></a><% } %>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuOperations') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <% if (canAccessModule('vehicles')) { %><a class="btn-outline" href="/admin/vehicles"><%= t('vehicles') %></a><% } %>
                <% if (canAccessModule('exports')) { %><a class="btn-outline" href="/records/exports"><%= t('exports') %></a><% } %>
                <% if (canAccessModule('water_tests')) { %><a class="btn-outline" href="/records/water-tests"><%= t('waterTestsTitle') %></a><% } %>
                <% if (canAccessModule('company_expenses')) { %><a class="btn-outline" href="/records/cleaning-routines"><%= t('cleaningRoutinesTitle') %></a><% } %>
                <a class="btn-outline" href="/worker"><%= t('workerView') %></a>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuFinance') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <% if (canAccessModule('credits')) { %><a class="btn-outline" href="/records/credits"><%= t('credits') %></a><% } %>
                <% if (canAccessModule('payment_ledger')) { %><a class="btn-outline" href="/records/payment-ledger"><%= t('paymentLedgerTitle') %></a><% } %>
                <% if (canAccessModule('reconciliation')) { %><a class="btn-outline" href="/records/reconciliation"><%= t('reconciliationTitle') %></a><% } %>
                <% if (canAccessModule('vehicle_expenses')) { %><a class="btn-outline" href="/records/vehicle-expenses"><%= t('vehicleExpensesTitle') %></a><% } %>
                <% if (canAccessModule('company_expenses')) { %><a class="btn-outline" href="/records/company-purchases"><%= t('companyPurchasesTitle') %></a><% } %>
                <% if (canAccessModule('jar_sales')) { %><a class="btn-outline" href="/records/jar-sales"><%= t('jarSalesTitle') %></a><% } %>
                <% if (canAccessModule('jar_sales')) { %><a class="btn-outline" href="/records/leakage-sales"><%= t('leakageJarSalesTitle') %></a><% } %>
                <% if (canAccessModule('rentals')) { %><a class="btn-outline" href="/records/rentals"><%= t('rentalsTitle') %></a><% } %>
                <% if (canAccessModule('savings')) { %><a class="btn-outline" href="/admin/savings"><%= t('savingsTitle') %></a><% } %>
                <% if (canAccessModule('vendor_aging')) { %><a class="btn-outline" href="/records/vendor-aging"><%= t('vendorAgingTitle') %></a><% } %>
                <% if (canAccessModule('invoices')) { %><a class="btn-outline" href="/records/customer-invoice"><%= t('customerInvoiceTitle') %></a><% } %>
                <% if (canAccessModule('invoices')) { %><a class="btn-outline" href="/records/vehicle-invoice"><%= t('vehicleInvoiceTitle') %></a><% } %>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuInventory') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <% if (canAccessModule('imports')) { %><a class="btn-outline" href="/records/imports"><%= t('importsTitle') %></a><% } %>
                <a class="btn-outline" href="/admin/import-item-types"><%= t('importItemTypesTitle') %></a>
                <a class="btn-outline" href="/admin/jar-types"><%= t('jarTypesTitle') %></a>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuReportsSettings') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body space-y-3">
              <div class="menu-section-grid">
                <% if (canAccessModule('history')) { %><a class="btn-outline" href="/records/history"><%= t('activityLogTitle') %></a><% } %>
                <a class="btn-outline" href="/admin/reports/all"><%= t('allReportsTitle') %></a>
                <a class="btn-outline" href="/admin/reports/payroll-summary"><%= t('payrollSummaryTitle') %></a>
                <a class="btn-outline" href="/admin/audit"><%= t('auditCenterTitle') %></a>
                <a class="btn-outline" href="/records/leakage-guard"><%= t('leakageGuardTitle') %></a>
                <a class="btn-outline" href="/admin/backup"><%= t('backup') %></a>
                <a class="btn-outline" href="/admin/settings"><%= t('settingsTitle') %></a>
                <a class="btn-outline" href="/admin/windows-kit"><%= t('windowsKitTitle') %></a>
                <a class="btn-outline" href="/admin/recycle-bin"><%= t('recycleBinTitle') %></a>
                <% if (isSuper) { %><a class="btn-outline" href="/admin/recovery"><%= t('recoveryTitle') %></a><% } %>
              </div>
              <div class="border-t border-slate-200 pt-3">
                <p class="text-xs font-semibold uppercase tracking-wide text-slate-500"><%= t('language') %> / <%= t('fontSize') %></p>
                <div class="mt-2 flex flex-wrap gap-2">
                  <a class="btn-outline <%= lang === 'en' ? 'bg-slate-100' : '' %>" href="/lang/en">EN</a>
                  <a class="btn-outline <%= lang === 'ne' ? 'bg-slate-100' : '' %>" href="/lang/ne">ने</a>
                  <a class="btn-outline <%= (calendarMode || 'AD') === 'AD' ? 'bg-slate-100' : '' %>" href="/calendar/ad">AD</a>
                  <% if (canConvertBs) { %>
                    <a class="btn-outline <%= (calendarMode || 'AD') === 'BS' ? 'bg-slate-100' : '' %>" href="/calendar/bs">BS</a>
                  <% } else { %>
                    <span class="btn-outline cursor-not-allowed opacity-60" title="Install nepali-date-converter and restart app">BS</span>
                  <% } %>
                  <button class="btn-outline font-size-toggle" type="button" data-font-size="sm" title="<%= t('fontSmall') %>">A-</button>
                  <button class="btn-outline font-size-toggle" type="button" data-font-size="md" title="<%= t('fontMedium') %>">A</button>
                  <button class="btn-outline font-size-toggle" type="button" data-font-size="lg" title="<%= t('fontLarge') %>">A+</button>
                  <button class="btn-outline font-size-toggle" type="button" data-font-size="xl" title="<%= t('fontXLarge') %>">A++</button>
                </div>
              </div>
            </div>
          </details>
        <% } else { %>
          <details class="menu-section" open>
            <summary class="menu-section-summary">
              <span><%= t('dashboard') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <a class="btn-outline" href="/worker"><%= t('home') %></a>
                <a class="btn-outline" href="/search"><%= t('search') %></a>
                <a class="btn-outline" href="/records/alerts"><%= t('alertCenterTitle') %></a>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuWorkforce') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <% if (canAccessModule('staffs')) { %><a class="btn-outline" href="/records/staffs"><%= t('staffsTitle') %></a><% } %>
                <% if (canAccessModule('attendance')) { %><a class="btn-outline" href="/records/staff-attendance"><%= t('staffAttendanceTitle') %></a><% } %>
                <a class="btn-outline" href="/records/profile"><%= t('profileTitle') %></a>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuOperations') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <% if (canAccessModule('vehicles')) { %><a class="btn-outline" href="/records/vehicles"><%= t('vehicles') %></a><% } %>
                <% if (canAccessModule('exports')) { %><a class="btn-outline" href="/records/exports"><%= t('exports') %></a><% } %>
                <% if (canAccessModule('water_tests')) { %><a class="btn-outline" href="/records/water-tests"><%= t('waterTestsTitle') %></a><% } %>
                <% if (canAccessModule('company_expenses')) { %><a class="btn-outline" href="/records/cleaning-routines"><%= t('cleaningRoutinesTitle') %></a><% } %>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuFinance') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <% if (canAccessModule('credits')) { %><a class="btn-outline" href="/records/credits"><%= t('credits') %></a><% } %>
                <% if (canAccessModule('payment_ledger')) { %><a class="btn-outline" href="/records/payment-ledger"><%= t('paymentLedgerTitle') %></a><% } %>
                <% if (canAccessModule('reconciliation')) { %><a class="btn-outline" href="/records/reconciliation"><%= t('reconciliationTitle') %></a><% } %>
                <% if (canAccessModule('vehicle_expenses')) { %><a class="btn-outline" href="/records/vehicle-expenses"><%= t('vehicleExpensesTitle') %></a><% } %>
                <% if (canAccessModule('company_expenses')) { %><a class="btn-outline" href="/records/company-purchases"><%= t('companyPurchasesTitle') %></a><% } %>
                <% if (canAccessModule('jar_sales')) { %><a class="btn-outline" href="/records/jar-sales"><%= t('jarSalesTitle') %></a><% } %>
                <% if (canAccessModule('jar_sales')) { %><a class="btn-outline" href="/records/leakage-sales"><%= t('leakageJarSalesTitle') %></a><% } %>
                <% if (canAccessModule('rentals')) { %><a class="btn-outline" href="/records/rentals"><%= t('rentalsTitle') %></a><% } %>
                <% if (canAccessModule('savings')) { %><a class="btn-outline" href="/records/savings"><%= t('savingsTitle') %></a><% } %>
                <% if (canAccessModule('vendor_aging')) { %><a class="btn-outline" href="/records/vendor-aging"><%= t('vendorAgingTitle') %></a><% } %>
                <% if (canAccessModule('invoices')) { %><a class="btn-outline" href="/records/customer-invoice"><%= t('customerInvoiceTitle') %></a><% } %>
                <% if (canAccessModule('invoices')) { %><a class="btn-outline" href="/records/vehicle-invoice"><%= t('vehicleInvoiceTitle') %></a><% } %>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuInventory') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body">
              <div class="menu-section-grid">
                <% if (canAccessModule('imports')) { %><a class="btn-outline" href="/records/imports"><%= t('importsTitle') %></a><% } %>
              </div>
            </div>
          </details>

          <details class="menu-section">
            <summary class="menu-section-summary">
              <span><%= t('menuReportsSettings') %></span>
              <svg xmlns="http://www.w3.org/2000/svg" class="menu-section-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="m6 9 6 6 6-6"/></svg>
            </summary>
            <div class="menu-section-body space-y-3">
              <div class="menu-section-grid">
                <% if (canAccessModule('history')) { %><a class="btn-outline" href="/records/history"><%= t('activityLogTitle') %></a><% } %>
              </div>
              <div class="border-t border-slate-200 pt-3">
                <p class="text-xs font-semibold uppercase tracking-wide text-slate-500"><%= t('language') %> / <%= t('fontSize') %></p>
                <div class="mt-2 flex flex-wrap gap-2">
                  <a class="btn-outline <%= lang === 'en' ? 'bg-slate-100' : '' %>" href="/lang/en">EN</a>
                  <a class="btn-outline <%= lang === 'ne' ? 'bg-slate-100' : '' %>" href="/lang/ne">ने</a>
                  <a class="btn-outline <%= (calendarMode || 'AD') === 'AD' ? 'bg-slate-100' : '' %>" href="/calendar/ad">AD</a>
                  <% if (canConvertBs) { %>
                    <a class="btn-outline <%= (calendarMode || 'AD') === 'BS' ? 'bg-slate-100' : '' %>" href="/calendar/bs">BS</a>
                  <% } else { %>
                    <span class="btn-outline cursor-not-allowed opacity-60" title="Install nepali-date-converter and restart app">BS</span>
                  <% } %>
                  <button class="btn-outline font-size-toggle" type="button" data-font-size="sm" title="<%= t('fontSmall') %>">A-</button>
                  <button class="btn-outline font-size-toggle" type="button" data-font-size="md" title="<%= t('fontMedium') %>">A</button>
                  <button class="btn-outline font-size-toggle" type="button" data-font-size="lg" title="<%= t('fontLarge') %>">A+</button>
                  <button class="btn-outline font-size-toggle" type="button" data-font-size="xl" title="<%= t('fontXLarge') %>">A++</button>
                </div>
              </div>
            </div>
          </details>
        <% } %>
      </div>
      </div>
  </div>
  <script>
    (function () {
      var toggle = document.getElementById('menuToggle');
      var panel = document.getElementById('menuPanel');
      if (!toggle || !panel) return;
      function initAccordion() {
        var sections = panel.querySelectorAll('.menu-section');
        if (!sections.length) return;
        sections.forEach(function (section) {
          if (section.dataset.ready === '1') return;
          section.dataset.ready = '1';
          section.addEventListener('toggle', function () {
            if (!section.open) return;
            sections.forEach(function (other) {
              if (other !== section) other.open = false;
            });
          });
        });
      }
      function openPanel() {
        initAccordion();
        panel.hidden = false;
        panel.classList.remove('hidden');
        toggle.setAttribute('aria-expanded', 'true');
      }
      function closePanel() {
        panel.classList.add('hidden');
        panel.hidden = true;
        toggle.setAttribute('aria-expanded', 'false');
      }
      toggle.addEventListener('click', function () {
        if (panel.hidden || panel.classList.contains('hidden')) {
          openPanel();
        } else {
          closePanel();
        }
      });
      panel.addEventListener('click', function (event) {
        if (event.target && event.target.dataset && event.target.dataset.close !== undefined) {
          closePanel();
        }
      });
      document.addEventListener('keydown', function (event) {
        if (event.key === 'Escape') closePanel();
      });
    })();
  </script>
<% } %>
<script>
  (function () {
    function setActive(buttons, matcher) {
      buttons.forEach(function (btn) {
        if (matcher(btn)) btn.classList.add('preference-toggle-active');
        else btn.classList.remove('preference-toggle-active');
      });
    }

    function initFontToggles() {
      var buttons = Array.prototype.slice.call(document.querySelectorAll('.font-size-toggle'));
      if (!buttons.length) return;
      function apply(size) {
        var safe = size || 'md';
        document.documentElement.dataset.fontSize = safe;
        try { localStorage.setItem('fontSize', safe); } catch (e) {}
        setActive(buttons, function (btn) { return btn.dataset.fontSize === safe; });
      }
      apply(document.documentElement.dataset.fontSize || 'md');
      buttons.forEach(function (btn) {
        btn.addEventListener('click', function () {
          apply(btn.dataset.fontSize || 'md');
        });
      });
    }

      function initThemeToggles() {
        var buttons = Array.prototype.slice.call(document.querySelectorAll('.theme-toggle'));
        if (!buttons.length) return;
        function apply(theme) {
          var allowed = ['classic', 'ocean', 'slate', 'sunrise', 'forest'];
          var safe = allowed.indexOf(theme) >= 0 ? theme : 'classic';
          document.documentElement.dataset.theme = safe;
          try { localStorage.setItem('themePreset', safe); } catch (e) {}
          document.querySelectorAll('input[name="theme_preset"]').forEach(function (input) {
            input.value = safe;
          });
          setActive(buttons, function (btn) { return btn.dataset.theme === safe; });
        }
      apply(document.documentElement.dataset.theme || 'classic');
      buttons.forEach(function (btn) {
        btn.addEventListener('click', function () {
          apply(btn.dataset.theme || 'classic');
        });
      });
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', function () {
        initFontToggles();
        initThemeToggles();
      });
    } else {
      initFontToggles();
      initThemeToggles();
    }
  })();
</script>
